notion-client>=2.0.0
python-dotenv>=1.0.0
httpx>=0.27.0
playwright>=1.40.0
# playwright-stealth: Stealth().apply_stealth_sync API used by gemini_keepalive.py
# (optional at runtime — keepalive degrades gracefully if absent)
playwright-stealth>=1.0.6
Pillow>=10.0.0
